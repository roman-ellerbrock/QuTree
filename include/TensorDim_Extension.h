#include "TensorDim.h"

namespace TensorDim_Extension {

	TensorDim ReplaceActive(const TensorDim& tdim, size_t mode, size_t dim);
	TensorDim ReplaceNtensor(const TensorDim& tdim, size_t ntensor);


}

